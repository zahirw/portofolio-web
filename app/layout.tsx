import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Zahir Wakid — Frontend Developer",
  description:
    "Portfolio of Zahir Wakid, a Frontend Developer with 4+ years building responsive, user-centric web and mobile apps in fintech, blockchain, and government projects.",
  openGraph: {
    title: "Zahir Wakid — Frontend Developer",
    description:
      "Frontend Developer specializing in React, Vue, Next.js and TypeScript.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
